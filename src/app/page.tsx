import VoiceInterface from '@/components/VoiceInterface';
// Bug fixes: free user paywall removed, Australia timeout fixed v2 - root dir fix

export default function Home() {
  return <VoiceInterface />;
}
